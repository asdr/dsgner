
(defpackage #:dsgner-asd
  (:use :cl :asdf))

(in-package #:dsgner-asd)

(defsystem dsgner
    :name "dsgner"
    :version "0.1"
    :depends-on ())

(defpackage :dsgner
  (:nicknames :dsgner)
  (:use :cl))

(in-package :dsgner)

(defmacro with-pure-design ((stream) &body body)
  (let ((f (car body))
	(r (cdr body)))
    (do (attribs nil nil)
	((null f))
      (if (and (not (consp (car r)))
	       (not (stringp (car r))))
	  (push (format nil "~A" (
  
			 